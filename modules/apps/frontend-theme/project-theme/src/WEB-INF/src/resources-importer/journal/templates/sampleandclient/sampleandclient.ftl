<div class="row container-fluid">
		<div class="col-md-12">
			<div class="row sample-space">
        <!-- SAMPLE PAGE  -->
				<div class="col-md-6">
					<h1 class="sample-page-title"> ${Text9tal.getData()} </h1>
					<#if SampleImage.getData()?? && SampleImage.getData() != ""> <img id="pTooltip" title="sample image" class="sample-page-img" alt="${SampleImage.getAttribute("alt")}" data-fileentryid="${SampleImage.getAttribute("fileEntryId")}" src="${SampleImage.getData()}" /> </#if> <p class="sample-page-p"> ${SampleParagraph.getData()} </p> <button type="button" class="btn"> Read More </button> </div> <!-- CLIENT RESPONSE --> <div class="col-md-6"> <h1 class="sample-page-title">${Textgcix.getData()}</h1> <!-- CLIENT #1 --> <div> <#if Client1Photo.getData()?? && Client1Photo.getData() != ""> <img id="pTooltip" title="client photo" class="client-photo" alt="${Client1Photo.getAttribute("alt")}" data-fileentryid="${Client1Photo.getAttribute("fileEntryId")}" src="${Client1Photo.getData()}" /> </#if> <p class="client-bio">${Client1Response.getData()}</p> <p class='client-signature'>${Client1SignatureWJobTitle.getData()} </div> <!-- CLIENT #2 --> <div> <#if Client2Photo.getData()?? && Client2Photo.getData() != ""> <img id="pTooltip" title="client photo" class="client-photo" alt="${Client2Photo.getAttribute("alt")}" data-fileentryid="${Client2Photo.getAttribute("fileEntryId")}" src="${Client2Photo.getData()}" /> </#if> <p class="client-bio">${Client2Response.getData()}</p> <p class='client-signature'>${Client2SignatureWJobTitle.getData()} </div> <!-- CLIENT #3 --> <div> <#if Client3Photo.getData()?? && Client3Photo.getData() != ""> <img id="pTooltip" title="client photo" class="client-photo" alt="${Client3Photo.getAttribute("alt")}" data-fileentryid="${Client3Photo.getAttribute("fileEntryId")}" src="${Client3Photo.getData()}" /> </#if> <p class="client-bio">${Client3Response.getData()}</p> <p class='client-signature'>${Client3SignatureWJobTitle.getData()} </div> </div> </div> </div> </div>